// test geodata utility dataset
#setenv POS=37.4205,-122.2046
#define RES=${SHELL gridlabd geodata -D utility location $POS -f RAW}
#define ANS=PACIFIC GAS & ELECTRIC CO.
#if ${RES} != ${ANS}
    #error "geodata -D address location ${ADDR}" is incorrect: expected ${ANS} but got ${RES} instead
#endif
