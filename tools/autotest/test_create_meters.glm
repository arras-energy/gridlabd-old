//////////////// Scenario 0: Load parent to meter ////////////////

#ifexist ../meters-withloads.glm
#define DIR=..
#endif

#for MODEL_NAME in "meters-withloads, nodes-withloads, notnodes-withloads, linkstometer, linkstotriplexmeter"

#gridlabd -C ${DIR:-.}/${MODEL_NAME}.glm -o ${DIR:-.}/${MODEL_NAME}.json
#python -m create_meters ${DIR:-.}/${MODEL_NAME}.json ${MODEL_NAME}-meters.json
#input "${MODEL_NAME}-meters.json"

#ifexist ../${MODEL_NAME}-meters.json
#on_exit 0 diff ../${MODEL_NAME}-meters.json ${MODEL_NAME}-meters.json
#endif

#done



